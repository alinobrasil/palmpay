version: '3.8'

services:
  palm-recognition-api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./registered_palms:/app/registered_palms
    environment:
      - REGISTERED_PALMS_DIR=/app/registered_palms
      - SIMILARITY_THRESHOLD=0.15
    restart: unless-stopped
